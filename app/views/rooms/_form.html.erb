<%= form_for(room) do |f| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :roomid %>
    <%= f.number_field :roomid %>
  </div>

  <div class="field">
    <%= f.label :size %>
    <%= f.select :size, [[4,4],[8,8],[12,12]] %>
  </div>

  <div class="field">
    <%= f.label :building %>
    <%= f.select :building, [['James B. Hunt','James B. Hunt'],['D. H. Hill','D. H. Hill']] %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
